# Development dependencies for CodeBase
# These are only needed for development, testing, and live reloading

# Testing framework
pytest>=7.0.0

# Live reloading for development
watchdog>=3.0.0

# Type checking (optional but recommended)
mypy>=1.0.0

# Code formatting (optional but recommended)
black>=23.0.0

# Linting (optional but recommended)
flake8>=6.0.0
